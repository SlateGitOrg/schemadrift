{
  "name": "schemadrift",
  "version": "0.1.0",
  "description": "A GraphQL breaking-change gate that downgrades severity using real field-usage telemetry, so the gate stays worth reading.",
  "type": "module",
  "private": true,
  "engines": { "node": ">=22.6" },
  "scripts": {
    "test": "node --test",
    "demo": "node src/demo.ts"
  }
}
